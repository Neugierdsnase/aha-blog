---
interface Props {
  quote: string;
  footer: string;
}

const { quote, footer } = Astro.props;
---

<tufte-epigraph>
  <blockquote>
    <p set:html={quote} />
    <footer set:html={footer} />
  </blockquote>
</tufte-epigraph>
