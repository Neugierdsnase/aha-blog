---
import { render } from "astro:content";
import ArticleLayout from "../../layouts/ArticleLayout.astro";
import TufteContent from "../../components/TufteContent.astro";
import { getContentPaths } from "../../lib/content-paths";

export const getStaticPaths = () => getContentPaths("test-content");

const post = Astro.props;
const { Content } = await render(post);
---

<ArticleLayout {...post.data} lang="en">
  <TufteContent>
    <Content />
  </TufteContent>
</ArticleLayout>
